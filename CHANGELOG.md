# Change Log

## v0.0.3

remove the compiled files and added the prepare command.

## v0.0.4

Change configure file in order to:

- Split type files into single folder
- Fit for both CommonJS module and ECMAScript module